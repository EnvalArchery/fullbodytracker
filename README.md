# Stable Pose Tracking

Bu proje webcam goruntusunde `mediapipe.tasks` Pose Landmarker ile iskelet cizer.

Kurulum:

```bash
pip install -r requirements.txt
```

Calistirma:

```bash
python main.py
```

Not:
- Ilk acilista pose modeli `models/mediapipe/` altina indirilir.
- `Q` ile cikis yapilir.
